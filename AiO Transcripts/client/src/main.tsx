import React from 'react'
import ReactDOM from 'react-dom/client'
import AppCyberpunkUI from './AppCyberpunkUI'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppCyberpunkUI />
  </React.StrictMode>,
)
