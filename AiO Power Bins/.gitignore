# Dependencies
node_modules/
client/node_modules/

# Build output (được sinh lại khi build)
dist/

# Cache / logs
*.log
.vite/
client/.vite/

# OS
Thumbs.db
.DS_Store

# Build staging + ZXP đã ký (sinh lại được)
build/

# Chứng chỉ ký (chứa private key — KHÔNG commit)
certs/

# FFmpeg binaries (bundle riêng, không commit — nặng)
bin/*.exe
bin/ffmpeg
bin/ffprobe

# SQLite DB dev
*.sqlite
*.sqlite-journal
